let mongoose = require('mongoose');
var moment = require('moment');

var Schema = mongoose.Schema;

let romSchema = mongoose.Schema({
/*
RoomStyle: Link to RoomStyle
RoomNumber: Integer,
isAvailable: Boolean
*/
})

let Rom = module.exports = mongoose.model('Rom', romSchema);
